# Veeam Recovery Orchestrator MCP Server Configuration

# VRO Server Connection
VRO_HOST=your-vro-server.example.com
VRO_USERNAME=administrator
VRO_PASSWORD=your-password-here

# TLS Configuration
# Set to "true" to reject unauthorized (self-signed) certificates
VRO_REJECT_UNAUTHORIZED=false

# Transport Configuration
# "stdio" (default) or "http"
VRO_TRANSPORT=stdio
# Port for HTTP transport mode
VRO_HTTP_PORT=3000
