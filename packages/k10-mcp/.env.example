# Kasten K10 MCP Server Configuration

# Path to kubeconfig file (optional, defaults to ~/.kube/config)
K10_KUBECONFIG=

# Kubernetes namespace where K10 is installed (default: kasten-io)
K10_NAMESPACE=kasten-io

# Kubernetes context to use (optional, uses current context if not set)
K10_CONTEXT=

# Transport Configuration
# "stdio" (default) or "http"
K10_TRANSPORT=stdio
# Port for HTTP transport mode
K10_HTTP_PORT=3004
